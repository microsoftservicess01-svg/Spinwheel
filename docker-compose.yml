version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/usr/src/app/data
    environment:
      - JWT_SECRET=change_this_secret
      - PORT=3000
      - DATABASE_FILE=/usr/src/app/data/app.db
    restart: unless-stopped
